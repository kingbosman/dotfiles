# .bashrc

# 1. Source global definitions (ESSENTIAL FOR FEDORA)
if [ -f /etc/bashrc ]; then
    . /etc/bashrc
fi

# 2. User specific environment (Standard Fedora Pathing)
if ! [[ "$PATH" =~ "$HOME/.local/bin:$HOME/bin:" ]]; then
    PATH="$HOME/.local/bin:$HOME/bin:$PATH"
fi

# 3. Custom Tools & Paths
export PATH="$PATH:$HOME/.config/composer/vendor/bin"
export PATH="$PATH:/usr/bin/go/bin"
export PATH="$PATH:$HOME/go/bin"
eval "$(zoxide init bash)"

# 4. Your History Search Shortcuts
bind '"\e[A": history-search-backward'
bind '"\e[B": history-search-forward'

# 5. Load your custom aliases
if [ -f ~/.config/bash/.bash_aliases ]; then
    . ~/.config/bash/.bash_aliases
fi

# 6. Fedora modularity (Loads any scripts in ~/.bashrc.d/)
if [ -d ~/.bashrc.d ]; then
    for rc in ~/.bashrc.d/*; do
        [ -f "$rc" ] && . "$rc"
    done
fi
